apiVersion: v1
kind: Service
metadata:
  name: aisec-api
  labels:
    app.kubernetes.io/name: aisec
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aisec
    app.kubernetes.io/component: api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http

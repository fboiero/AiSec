apiVersion: v1
kind: Secret
metadata:
  name: aisec-secrets
  labels:
    app.kubernetes.io/name: aisec
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Replace with actual values before deploying
  AISEC_SECRET_KEY: "CHANGE-ME-generate-a-random-secret"
  # Cloud storage credentials (uncomment as needed)
  # AWS_ACCESS_KEY_ID: ""
  # AWS_SECRET_ACCESS_KEY: ""
  # GOOGLE_APPLICATION_CREDENTIALS: ""
  # AZURE_STORAGE_CONNECTION_STRING: ""
